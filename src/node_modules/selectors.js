import * as R from 'ramda'

const getBookById = (state,id) => R.prop(id,state.books)
export const getBooks = state => {
    const books =R.map(id => getBookById(state,id),state.booksPage.ids)
    return books
}
export const getRenderedBooksLength = state => R.length(state.booksPage.ids)

